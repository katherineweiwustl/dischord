import styled from 'styled-components'

export const Container = styled('div')`

    display: flex;
    justify-content: space-between;
    /* horizontally adjusts elements (if flex is columns then it's vertically adjusting elements) */
    align-items: center;
    /* vertically adjusts elements */
    
`;